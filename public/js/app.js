// Example JavaScript code with Tailwind CSS utility classes

document.addEventListener('DOMContentLoaded', function() {
    const appDiv = document.getElementById('app');
    if (appDiv) {
        appDiv.innerHTML = `
            <p class="text-lg text-gray-700">This is a dynamically added paragraph with Tailwind CSS classes.</p>
        `;
    }
});